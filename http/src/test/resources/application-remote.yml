server:
  port: 8443
logging:
  level:
    at.asitplus: DEBUG
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
  datasource:
    url: "jdbc:h2:mem:userstore"
  test:
    mockmvc:
      print: log_debug
  config:
    import:
  cloud:
    config:
      enabled: false
backend:
  public-context: "http://localhost:8443/"
  credentials:
    lifetime: P1D
    one-credential-per-device-binding: true
  remote-crypto:
    enabled: true
    hostname: "rs-decafbad.ps.prime-sign.com"
    port: 443
    api-key: "49DOY-X3RLK-W12LW-MKDP5-X9LMH"
  issuer-key:
    type: REMOTE
    remote:
      key-name: "032qukjd"
      certificate: "classpath:032qukjd.cer"
